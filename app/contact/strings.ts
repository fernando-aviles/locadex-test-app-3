import { msg } from "gt-next";

// Strings specific to the Contact page
export const contactStrings = {
  heading: msg("Get in Touch"),
  intro: msg("Have a question or want to work together? We'd love to hear from you."),
  emailLabel: msg("Email"),
  emailValue: "hello@locadex.dev",
  locationLabel: msg("Location"),
  locationValue: msg("San Francisco, CA"),
  formNamePlaceholder: msg("Your name"),
  formEmailPlaceholder: msg("Your email"),
  formMessagePlaceholder: msg("Your message"),
  formSubmitButton: msg("Send Message"),
};
